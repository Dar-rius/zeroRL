name: Security & Supply Chain

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  security-events: write

jobs:
  security-checks:
    name: Vulnerability Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          format: spdx-json
          output-file: zerorl.sbom.json

      - name: Security audit (OSV-Scanner)
        uses: google/osv-scanner-action@v1.7.1
        with:
          scan-args: |-
            --lockfile=uv.lock
